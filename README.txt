Johnny’s Chat Bot
For C.S 1.0 

This is a Chat Bot that is interacted within the terminal. The Chat Bot will tell the user what games Johnny plays within whatever genre that the user inputs. The Chat Bot first checks to see if Johnny even plays any games within the genre that the user inputted, if so then the Chat Bot will randomly select a game from a list of the genre and respond to the user with that game. The Chat Bot will repeatedly ask the user to input a game genre until the user quits out.